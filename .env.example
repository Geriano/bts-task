POSTGRES_DB=spring
POSTGRES_USER=root
POSTGRES_PASSWORD=change-me
# Generate with: openssl rand -base64 32   (do NOT ship the change-me value)
APP_JWT_SECRET=change-me-replace-with-openssl-rand-base64-32-output